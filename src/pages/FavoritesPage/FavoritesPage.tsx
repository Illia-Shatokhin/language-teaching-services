import { FC, ReactElement } from 'react';
import { Title } from './FavoritesPage.styled';

const FavoritesPage: FC = (): ReactElement => {
  return <Title>Favorites Page</Title>;
};

export default FavoritesPage;
